## Verification Loop Limit
If a sub-phase fails its own verification checks 3 times in a row,
stop retrying the same implementation approach. Flag ESCALATE as the
recommended path in the next Verification Summary and summarize what
was attempted and why it failed, instead of continuing to iterate.
